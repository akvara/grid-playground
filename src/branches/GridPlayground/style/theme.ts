import createMuiTheme from '@material-ui/core/styles/createMuiTheme';
import { blue, blueGrey, indigo, lime, pink, teal } from '@material-ui/core/colors';

export const theme = createMuiTheme({
  palette: {
    primary: teal,
  },
  typography: {
    // useNextVariants: true,
  },
});