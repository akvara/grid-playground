export const pageTitle = 'A News';

export const nasdaq = {
  dateFormat: 'YYYY.MM.DD',
  before: {
    amount: 3,
    unit: 'month',
  },
};

const constants = {
  nasdaq,
};

export default constants;
